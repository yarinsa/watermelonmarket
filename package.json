{
  "name": "watermelon-market",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "start": "cd backend && ts-node src/server.ts",
    "build": "yarn build-frontend && yarn build-backend",
    "build-frontend": "cd frontend && yarn install && yarn build && cd -",
    "build-backend": "cd backend && yarn install && yarn build && cd -"
  },
  "engines": {
    "yarn": "1.x"
  }
}
